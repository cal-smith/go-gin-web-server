databases:
  - name: go-gin-db
    databaseName: go-gin-db
    user: mytestsite
    plan: basic-256

services:
  - type: web
    name: go-gin
    env: go
    buildCommand: "go build -tags netgo -ldflags '-s -w' -o app"
    startCommand: "./app"
    autoDeploy: false
    plan: standard
